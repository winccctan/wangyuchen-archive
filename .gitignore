# 依赖与私有配置
node_modules/
scraper/.env
scraper/cron.log

# 系统文件
.DS_Store

# 数据备份（重解析前快照，体积大，不入库）
site/data/*.bak

# 项目级 agent 记忆（不入库）
scraper/.workbuddy/

# 补档断点游标（本机状态，不入库）
scraper/.live-cursor.json
scraper/.live-cursor.json.tmp
scraper/data/

# 粉丝档案原始抓取缓存（含 uid 名单，绝不提交）
.cache/

# 私有导出与工具（含 uid 明细的榜单 / 匹配表，绝不提交）
private-data/

# 第三方榜单覆盖表（map 的 key 就是 uid）。
# ⚠️ 2026-09-23 起这些表只存 D1（gift_override），仓库里一份都不留 —— CI 从 D1 读。
data/fans-2026-gift.json
data/fans-2024plus-gift.json
data/fans-2026-gift-hashed.json

# 人工锁定的昵称→uid 归属表（含 uid，绝不提交）
data/nick-uid-pins.json
